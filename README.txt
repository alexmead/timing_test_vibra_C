C project which is designed to be used to query the AT commands on the WP76XX
so we can get a hopefully millisecond accurate timing offset between the 
Legato/Yocto OS time and the cellular network time. 

This is a first pass to test the offset nature of the method. 